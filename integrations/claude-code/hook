#!/usr/bin/env bash
#
# The Claude Code adapter: everything this plugin knows about Claude Code.
#
#   tama hook claude-code <event> [args]
#
# Wired once from the user's `settings.json` — see README.md next to this file for
# the block to paste. Nothing about Claude Code appears in that block beyond the
# names of its own events, and nothing about Claude Code appears anywhere else in
# the plugin: which events matter, what a delegated run means, and how to find the
# one field the payload has to give up all live here (ADR-0002). A fix reaches the
# user by pulling the plugin.
#
# Events, and what the user sees because of them:
#
#   SessionStart                       state idle     — an agent is in this pane now
#   UserPromptSubmit                   state running
#   PreToolUse, PostToolUse            state running  — the hot path, on every call
#   PostToolUseFailure, PostToolBatch  state running
#   PermissionRequest                  state waiting  — blocked on the user
#   Notification (the types that mean the user is wanted)
#                                      state waiting
#   Stop                               state idle
#   StopFailure                        state error    — the turn died on an API error
#   SubagentStart / SubagentStop       state subagent-start|stop <agent_id>
#   SessionEnd                         state clear    — no trace left in tmux
#
# Anything else is ignored in silence, on purpose: Claude Code grows events on its
# own schedule, and a user whose settings wire one this version has never heard of
# must not have their turn fail over it.
#
# Why `waiting` comes from PermissionRequest and not from a matcher on
# Notification: Claude Code has a dedicated event for the thing that blocks a
# turn, so the user's config stays a list of event names with no filtering
# knowledge in it. Notification is wired too, because it is the only event behind
# `AskUserQuestion` and the idle prompt, and there the type does have to be looked
# at — auth_success and the elicitation results are routine noise and must not
# turn a pane into one that wants the user.
#
# What a delegated run means here: a subagent's own end is SubagentStop, never
# Stop — Claude Code converts it — so `state idle` cannot be reported by a
# delegated run in the first place. Subagent events therefore only ever move the
# subagent counter, which is what makes an agent whose own turn has finished while
# its subagents work render as working in the background rather than as finished.
# A subagent's tool calls do report `running`, which is true: something is working
# in that pane. Keeping a delegated run's *notification* quiet is a separate
# decision and belongs with the notification half.
#
# Failure policy, inherited from the CLI: a wrong invocation exits 2 on stderr;
# everything else exits 0 in silence. This runs inside an agent's turn, so it must
# never be the reason the turn fails.

set -o nounset

# Exported by bin/tama, which is the only thing that runs this file. Missing means
# something invoked the adapter directly, which is not a user's mistake to be
# shouted about from inside a turn.
[ -n "${TAMA_BIN:-}" ] || exit 0

TAMA_CC_AGENT='claude-code'

# ---------------------------------------------------------------------------
# The payload
#
# Claude Code delivers its hook input as JSON on stdin, and there is no other way
# to get at it: as of 2.1.227 a command hook substitutes only ${CLAUDE_PROJECT_DIR}
# and its two siblings into `command`/`args`, so no field of the payload can be
# handed over as an argument. `agent_id` is the id the plugin counts subagents by,
# and the notification type is what separates a question for the user from
# routine noise, so those two are read here — and nothing else is.
#
# This is the residue ADR-0001 predicted and put in `integrations/`: the *core*
# CLI takes arguments and never reads stdin, and `jq` is not a dependency of the
# plugin. So the extraction below is deliberately not a JSON parser — it is two
# string matches with no subprocess, on two fields whose values are opaque tokens
# with no escaping in them. A field it cannot find is a field the adapter does
# without, quietly, which is the whole of its error handling.
# ---------------------------------------------------------------------------

TAMA_CC_PAYLOAD=''
TAMA_CC_PAYLOAD_READ='no'
TAMA_CC_FIELD=''

# Reads stdin once, into TAMA_CC_PAYLOAD. Only the events that need a field ever
# call it, so the hot path — a tool call reporting `running` — never touches
# stdin at all.
#
# A terminal on stdin means a human is running this by hand rather than Claude
# Code feeding it: reading would hang, and hanging inside a hook is worse than
# knowing nothing. The timeout is the same bet for a pipe that was opened and
# never written to.
cc_read_payload() {
  [ "$TAMA_CC_PAYLOAD_READ" = 'no' ] || return 0
  TAMA_CC_PAYLOAD_READ='yes'
  [ -t 0 ] && return 0
  # -d '' reads to EOF, since a JSON payload contains no NUL; the non-zero status
  # that EOF returns is the normal case and not a failure. Whole seconds because
  # bash 3.2 — the /bin/bash every macOS ships — cannot do fractional -t.
  IFS= read -r -d '' -t 5 TAMA_CC_PAYLOAD || true
  return 0
}

# The value of a top-level JSON string field, in TAMA_CC_FIELD. Non-zero, and the
# field left empty, when the payload does not carry it.
#
# Scoped to the two opaque-token fields this adapter reads, so it does not have to
# understand escapes, numbers, or nesting: it finds the name, steps over the colon
# and the space Claude Code may or may not print, and takes what is between the
# next two quotes.
cc_field() { # <name>
  TAMA_CC_FIELD=''
  cc_read_payload

  local rest="$TAMA_CC_PAYLOAD"
  case "$rest" in
    *"\"$1\""*) rest="${rest#*\""$1"\"}" ;;
    *) return 1 ;;
  esac

  rest="${rest#*:}"
  # Leading whitespace, one character at a time: bash 3.2 has no extglob-free way
  # to strip a run of it, and there is never more than a space or two here.
  while :; do
    case "$rest" in
      [[:space:]]*) rest="${rest#?}" ;;
      *) break ;;
    esac
  done

  # Anything but a string here — null for an absent id, a number, an object — is
  # not a value this adapter can use.
  case "$rest" in
    '"'*) rest="${rest#\"}" ;;
    *) return 1 ;;
  esac

  TAMA_CC_FIELD="${rest%%\"*}"
  [ -n "$TAMA_CC_FIELD" ]
}

# ---------------------------------------------------------------------------
# The events
# ---------------------------------------------------------------------------

# The core, reached the way any hand-wired agent reaches it. Every call names the
# agent, because the name is sticky in the pane record and a session that starts
# with `SessionStart` disabled would otherwise never record one. `clear` takes no
# name — it is removing the one that is there.
cc_state() { # <state> [args…]
  exec "$TAMA_BIN" state "$1" "$TAMA_CC_AGENT" "${@:2}"
}

# A notification that means the user is wanted, rather than one that means
# something routine happened. An opt-in list: a type this version has not seen is
# treated as routine, because a pane wrongly stuck in `waiting` outlives the event
# that caused it, while a missed one is corrected by the next thing the agent does.
cc_notification_wants_user() { # <type>
  case "$1" in
    permission_prompt | idle_prompt | agent_needs_input) return 0 ;;
    elicitation_dialog | elicitation_url_dialog) return 0 ;;
    *) return 1 ;;
  esac
}

main() {
  # The router already refused an empty event, so this is only reachable by
  # invoking the adapter directly.
  if [ "$#" -eq 0 ] || [ -z "$1" ]; then
    printf 'tama: hook claude-code needs an event\n' >&2
    exit 2
  fi

  local event="$1"
  shift

  case "$event" in
    SessionStart)
      cc_state idle "$@"
      ;;
    UserPromptSubmit | PreToolUse | PostToolUse | PostToolUseFailure | PostToolBatch)
      cc_state running "$@"
      ;;
    PermissionRequest)
      cc_state waiting "$@"
      ;;
    Notification)
      # Without a type there is nothing to tell noise from a question, and
      # guessing wrong leaves a pane asking for the user forever.
      cc_field notification_type || exit 0
      cc_notification_wants_user "$TAMA_CC_FIELD" || exit 0
      cc_state waiting "$@"
      ;;
    Stop)
      cc_state idle "$@"
      ;;
    StopFailure)
      cc_state error "$@"
      ;;
    SubagentStart | SubagentStop)
      # The counter is keyed by the id Claude Code gives the delegated run, so a
      # payload without one leaves nothing to count. Ignoring that is safe in the
      # direction that matters: no subagent is tracked, rather than a pane pinned
      # to `background` by an id nothing will ever stop.
      cc_field agent_id || exit 0
      case "$event" in
        SubagentStart) exec "$TAMA_BIN" state subagent-start "$TAMA_CC_FIELD" "$@" ;;
        *) exec "$TAMA_BIN" state subagent-stop "$TAMA_CC_FIELD" "$@" ;;
      esac
      ;;
    SessionEnd)
      exec "$TAMA_BIN" state clear "$@"
      ;;
    # An event this version does not map. Silent on purpose: Claude Code adds
    # events, and a settings.json written against a newer plugin has to be
    # harmless on an older one.
    *)
      exit 0
      ;;
  esac
}

main "$@"
